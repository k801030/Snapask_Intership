<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:gravity="center_vertical">
    <ImageView
        android:id="@+id/icon"
        android:layout_width="25dp"
        android:layout_height="wrap_content"
        android:src="@drawable/info_mail"
        android:scaleType="fitCenter"
        android:layout_margin="@dimen/fragment_item_info_padding"/>


    <LinearLayout
        android:id="@+id/content_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:layout_toRightOf="@id/icon">
        <TextView
            android:id="@+id/content"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/fragment_item_info_padding"
            android:textSize="@dimen/fragment_info_text_size"
            android:textColor="@color/main_text_color"
            android:text="kk801030@gmail.com" />

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="right|center_vertical"
            android:textSize="@dimen/fragment_info_text_size"
            android:layout_margin="@dimen/fragment_item_info_padding"
            android:textColor="@color/sub_text_color"
            android:text="Email" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/horizontal_line"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@id/content_layout"/>



</RelativeLayout>